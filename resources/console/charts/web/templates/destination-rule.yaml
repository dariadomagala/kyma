apiVersion: "networking.istio.io/v1alpha3"
kind: "DestinationRule"
metadata:
  name: console-web
spec:
  host: console-web.{{ .Release.Namespace }}.svc.cluster.local
  trafficPolicy:
    tls:
      mode: DISABLE