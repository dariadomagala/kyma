kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: pamela
spec:
  rules:
  - host: "*.kyma.local"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: pamela
            port:
              number: 3001 